# 🚀 كيفية تشغيل الخادم بدون Console

## الطريقة 1: استخدام ملف .bat (الأسهل!)

### خطوات:
1. **انقر نقراً مزدوجاً** على ملف `START_SERVER.bat`
2. انتظر حتى يفتح نافذة سوداء
3. الخادم سيعمل تلقائياً!

### إذا ظهرت رسالة خطأ:
- انقر نقراً مزدوجاً على `CREATE_ENV_AND_START.bat` بدلاً منه
- هذا الملف سينشئ ملف `.env` تلقائياً ثم يشغل الخادم

## الطريقة 2: استخدام PowerShell

1. **انقر بزر الماوس الأيمن** على `START_SERVER.ps1`
2. اختر **"Run with PowerShell"** أو **"تشغيل باستخدام PowerShell"**
3. إذا ظهرت رسالة أمان، اضغط **"Y"** أو **"نعم"**

## الطريقة 3: استخدام File Explorer

1. افتح مجلد `server`
2. اضغط على شريط العنوان
3. اكتب: `cmd` واضغط Enter
4. اكتب: `npm start` واضغط Enter

## ⚠️ إذا لم يعمل:

### تحقق من:
1. **Node.js مثبت؟**
   - افتح Command Prompt
   - اكتب: `node --version`
   - إذا ظهر رقم إصدار، Node.js مثبت ✅

2. **npm مثبت؟**
   - اكتب: `npm --version`
   - إذا ظهر رقم إصدار، npm مثبت ✅

3. **ملف .env موجود؟**
   - افتح مجلد `server`
   - تأكد من وجود ملف `.env`
   - إذا لم يكن موجوداً، انقر على `CREATE_ENV_AND_START.bat`

## 📋 الملفات المتوفرة:

- ✅ `START_SERVER.bat` - تشغيل الخادم مباشرة
- ✅ `CREATE_ENV_AND_START.bat` - إنشاء .env ثم تشغيل الخادم
- ✅ `START_SERVER.ps1` - نسخة PowerShell

## 🎯 الأسهل:

**انقر نقراً مزدوجاً على `START_SERVER.bat`** ✅

---

**بعد تشغيل الخادم، افتح المتصفح على:**
```
http://localhost:3000/api/health
```

يجب أن ترى:
```json
{"status":"ok","message":"Server is running"}
```

