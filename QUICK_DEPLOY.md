# 🚀 دليل سريع لرفع الموقع على InfinityFree

## المشكلة
المجلد `al3adrar.great-site.net/htdocs` غير موجود على الخادم.

## الحل السريع (3 خطوات)

### 1️⃣ إنشاء المجلد على الخادم

1. سجل الدخول إلى [لوحة تحكم InfinityFree](https://infinityfree.net/)
2. اذهب إلى **File Manager**
3. أنشئ مجلد باسم `htdocs` (أو الاسم المطلوب حسب إعدادات النطاق)

### 2️⃣ تحضير الملفات محلياً

شغّل السكريبت التالي في PowerShell:

```powershell
.\copy-to-htdocs.ps1
```

هذا سينسخ جميع الملفات المطلوبة إلى مجلد `htdocs/` محلياً.

### 3️⃣ رفع الملفات

#### الطريقة الأولى: File Manager (الأسهل)
1. في File Manager، افتح مجلد `htdocs`
2. اضغط **Upload**
3. ارفع جميع الملفات من مجلد `htdocs/` المحلي

#### الطريقة الثانية: FTP
1. احصل على معلومات FTP من لوحة التحكم
2. استخدم FileZilla
3. ارفع جميع الملفات إلى المجلد الصحيح

## الملفات المطلوبة

✅ `index.html` (✅ تم إصلاحه - موجود في `htdocs/`)
✅ `.htaccess` (✅ تم تحسينه - موجود في `htdocs/`)
✅ جميع ملفات JavaScript (main.dart.js, flutter.js, flutter_bootstrap.js, etc.)
✅ جميع المجلدات (assets, canvaskit, icons)

## بعد الرفع

1. افتح: `https://al3adrar.great-site.net`
2. يجب أن يظهر الموقع!

## ⚠️ ملاحظة مهمة

InfinityFree **لا يدعم Node.js**. إذا كنت تحتاج إلى API backend:
- استخدم PHP (سيتم إنشاء ملفات PHP قريباً)
- أو استخدم خدمة خارجية مثل Render/Railway لـ Node.js

## للمزيد من التفاصيل

راجع ملف `DEPLOY_INFINITYFREE.md` للدليل الكامل.

